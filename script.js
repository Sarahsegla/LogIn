// make sure code run once the page is ready
$(document).ready(function() {
    // selecting the 'login' button by class for a click function
$('.logButton').click(function() {
    // selecting the whole modal by class to show once being clicked
    $('.wholeModal').toggle()
}) 
});